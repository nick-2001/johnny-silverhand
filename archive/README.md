# 历史素材

此目录保留旧版本和实验过程，不作为安装入口。

- `chibi/`：早期 Q 版全身版本；原先两张内容完全相同的精灵图已合并。
- `relic-original/`：2026-09-15 裁切修复前的清单和精灵图。
- `v2-workbench/`：原 `johnny-silverhand-v2/`，仅完成九行标准动作的恢复，未完成 v2 的完整交付。`final/` 是当时的中间输出。

历史 JSON 中的项目路径已改为仓库相对路径；图像生成任务记录是溯源信息，不能保证外部任务链接仍可用。`qa/recover_existing.py` 使用相对于脚本的路径，需要 Pillow；运行会重新生成该工作目录中的逐帧素材。
